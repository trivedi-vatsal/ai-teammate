name: Test AI Teammate

on:
  pull_request:
    types: [opened, synchronize, reopened]
    branches: [main]

jobs:
  test-local-action:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      pull-requests: write

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js 20
        uses: actions/setup-node@v4
        with:
          node-version: "20"
          cache: "npm"

      - name: Install dependencies and build
        run: |
          npm ci
          npm run build

      - name: Test AI PR Review (expert)
        uses: ./
        with:
          azure_openai_endpoint: ${{ secrets.AZURE_OPENAI_ENDPOINT }}
          azure_openai_api_key: ${{ secrets.AZURE_OPENAI_API_KEY }}
          azure_openai_model_name: ${{ secrets.AZURE_OPENAI_MODEL_NAME }}
          review_depth: "expert"
          temperature: "0.5"

      # - name: Test AI PR Review (comprehensive)
      #   uses: ./
      #   with:
      #     azure_openai_endpoint: ${{ secrets.AZURE_OPENAI_ENDPOINT }}
      #     azure_openai_api_key: ${{ secrets.AZURE_OPENAI_API_KEY }}
      #     azure_openai_model_name: ${{ secrets.AZURE_OPENAI_MODEL_NAME }}
      #     review_depth: "comprehensive"
      #     max_tokens: "2000"
      #     temperature: "0.5"

      # - name: Test AI PR Review (basic)
      #   uses: ./
      #   with:
      #     azure_openai_endpoint: ${{ secrets.AZURE_OPENAI_ENDPOINT }}
      #     azure_openai_api_key: ${{ secrets.AZURE_OPENAI_API_KEY }}
      #     azure_openai_model_name: ${{ secrets.AZURE_OPENAI_MODEL_NAME }}
      #     review_depth: "basic"
      #     max_tokens: "2000"
      #     temperature: "0.5"
